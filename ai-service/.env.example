# Copy to ai-service/.env (never commit .env)

# OpenAI ChatGPT — https://platform.openai.com/api-keys
AI_PROVIDER=openai
OPENAI_API_KEY=sk-proj-your-key-here
OPENAI_MODEL=gpt-4o-mini

# Offline FAQ only (no key):
# AI_PROVIDER=faq
